# Fix Plan — 2026-03-05

## Fix #1 — Missing try/catch in LoadRaceBox1Button_Click
- Status: **COMPLETE** (2026-03-06)
- File: `src/CastleOverlayV2/MainForm.cs` — `LoadRaceBox1Button_Click`
- Change: Wrapped entire load body (after path null-guard) in try/catch matching
  the Castle handler pattern. `UpdateRunTypeLockState()` remains outside the
  try/catch. Catch logs via `Logger.Log` and shows a user-facing `MessageBox`.
- Branch: `feature/fix-racebox-load-handlers`

## Fix #2 — Missing try/catch in LoadRaceBox3Button_Click
- Status: **COMPLETE** (2026-03-06)
- File: `src/CastleOverlayV2/MainForm.cs` — `LoadRaceBox3Button_Click`
- Change: Same pattern as Fix #1 applied to the Slot 3 handler.
- Branch: `feature/fix-racebox-load-handlers`

## Fix #3 — PlotManager switch expression missing default arm
- Status: **COMPLETE** (2026-03-06)
- File: `src/CastleOverlayV2/Plot/PlotManager.cs:339`
- Change: Added `_ => throttleAxis` as the default arm of the `channelLabel` switch
  expression to prevent `SwitchExpressionException` for unknown channel labels.
- Branch: `feature/fix-switch-default-arm`
